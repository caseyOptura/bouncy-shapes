repos:
  - repo: local
    hooks:
      - id: check-file-size
        name: Check file size (HTML/JS max 400 lines)
        entry: .pre-commit-hooks/check-file-size.sh
        language: script
        types_or: [html, javascript]
        exclude: ^reports/
